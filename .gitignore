.DS_Store
tmp/
secrets/
.env
.env.*
!.env.example

# Terraform local state must never be committed.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Migration bundles may contain private Mattermost state.
*.tar.gz
*.zip
records/
